import { Matrix4 } from "./Matrix4"

export class RotationX extends Matrix4 {
  /**
   * Constructs an Euler rotation matrix for rotation around the axis x.
   * @param {number} angle_γ Angle gamma (in radians) around the axis x.
   */
  constructor(/* … */) {
    super(/* … */)
  }

  /**
   * Set an Euler rotation matrix for rotation around the axis x.
   * @param {number} angle_γ Angle gamma (in radians) around the axis x.
   */
  public set(/* … */) {
    super.set(/* … */)
  }
}
