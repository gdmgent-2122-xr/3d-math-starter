import { Matrix4 } from "./Matrix4"

export class RotationY extends Matrix4 {
  /**
   * Constructs an Euler rotation matrix for rotation around the axis y.
   * @param {number} angle_β Angle beta (in radians) around the axis y.
   */
  constructor(/* … */) {
    super(/* … */)
  }

  /**
   * Set an Euler rotation matrix for rotation around the axis y.
   * @param {number} angle_β Angle beta (in radians) around the axis y.
   */
  public set(/* … */) {
    super.set(/* … */)
  }
}
