import { Matrix4 } from "./Matrix4"

export class RotationZ extends Matrix4 {
  /**
   * Constructs an Euler rotation matrix for rotation around the axis z.
   * @param {number} angle_α Angle alpha (in radians) around the axis z.
   */
  constructor(/* … */) {
    super(/* … */)
  }

  /**
   * Set an Euler rotation matrix for rotation around the axis z.
   * @param {number} angle_α Angle alpha (in radians) around the axis z.
   */
  public set(/* … */) {
    super.set(/* … */)
  }
}
