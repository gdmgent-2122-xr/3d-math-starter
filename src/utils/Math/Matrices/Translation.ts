import { Matrix4 } from "./Matrix4"

export class Translation extends Matrix4 {
  /**
   * Constructs a translation matrix.
   * @param {number} translate_x Translate along the axis x.
   * @param {number} translate_y Translate along the axis y.
   * @param {number} translate_z Translate along the axis z.
   */
  constructor(/* … */) {
    super(/* … */)
  }

  /**
   * Set a translation matrix.
   * @param {number} translate_x Translate along the axis x.
   * @param {number} translate_y Translate along the axis y.
   * @param {number} translate_z Translate along the axis z.
   */
  public set(/* … */) {
    super.set(/* … */)
  }
}
