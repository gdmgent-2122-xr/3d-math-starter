import { Matrix4 } from "./Matrix4"

export class Scale extends Matrix4 {
  /**
   * Constructs a scale matrix.
   * @param {number} scale_x Scale along the axis x.
   * @param {number} scale_y Scale along the axis y.
   * @param {number} scale_z Scale along the axis z.
   */
  constructor(/* … */) {
    super(/* … */)
  }

  /**
   * Set a scale matrix.
   * @param {number} scale_x Scale along the axis x.
   * @param {number} scale_y Scale along the axis y.
   * @param {number} scale_z Scale along the axis z.
   */
  public set(/* … */) {
    super.set(/* … */)
  }

  /**
   * Set a uniform scale matrix.
   * @param {number} scale Scale along the axes.
   */
  public setUniform(/* … */) {
    super.set(/* … */)
  }
}
